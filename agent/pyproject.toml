[project]
name = "agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "accelerate>=1.2.1",
    "mypy>=1.14.1",
    "ruff>=0.8.4",
    "smolagents>=1.0.0",
]
